:root {
  --navy: #152846;
  --navy-soft: #243d63;
  --gold: #c79f52;
  --text: #182742;
  --muted: #4d5f79;
  --line: rgba(24, 39, 66, 0.10);
  --soft: #f7f8fb;
  --shadow: 0 18px 48px rgba(10, 20, 35, 0.08);
  --container: 1260px;
}

* { box-sizing: border-box; }
html { scroll-behavior: smooth; }
body {
  margin: 0;
  color: var(--text);
  background: #ffffff;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}
a { text-decoration: none; color: inherit; }
button, input, textarea { font: inherit; }
button:disabled { opacity: .75; cursor: not-allowed; }
.container { width: min(var(--container), calc(100% - 56px)); margin: 0 auto; }

.topbar {
  position: sticky;
  top: 0;
  z-index: 30;
  background: rgba(255,255,255,.96);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid rgba(199,159,82,.24);
}
.topbar-inner {
  min-height: 98px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
}
.brand-wrap { display: flex; align-items: center; gap: 16px; }
.brand-mark {
  width: 62px;
  height: 62px;
  border-radius: 18px;
  border: 2px solid var(--gold);
  background: linear-gradient(180deg, #173760 0%, #0d2140 100%);
  color: #f0ddb6;
  display: grid;
  place-items: center;
  box-shadow: 0 10px 24px rgba(0,0,0,.08);
}
.brand-mark span {
  font-family: Georgia, "Times New Roman", serif;
  font-size: 2rem;
  font-weight: 700;
}
.brand-copy { display: flex; flex-direction: column; }
.brand {
  color: var(--navy);
  font-family: Georgia, "Times New Roman", serif;
  font-size: 2.15rem;
  line-height: 1;
  letter-spacing: .02em;
  font-weight: 700;
}
.brand-sub {
  color: rgba(21,40,70,.82);
  font-size: .88rem;
  letter-spacing: .35em;
  margin-top: 8px;
}
.nav { display: flex; gap: 34px; }
.nav a {
  color: var(--navy);
  font-size: 1.08rem;
  font-weight: 600;
}

.hero {
  padding: 46px 0 54px;
  background: #ffffff;
  border-bottom: 1px solid var(--line);
}
.hero-grid {
  display: grid;
  grid-template-columns: minmax(0, .92fr) minmax(0, 1.08fr);
  gap: 40px;
  align-items: center;
}
.hero-copy { min-width: 0; }
.badge {
  display: inline-flex;
  border: 1px solid rgba(199,159,82,.86);
  color: var(--navy);
  background: #ffffff;
  border-radius: 999px;
  padding: 12px 22px;
  font-size: 1rem;
  text-transform: uppercase;
  letter-spacing: .18em;
  font-weight: 700;
}
.hero h1 {
  margin: 24px 0 0;
  color: var(--navy);
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(2.6rem, 4.3vw, 4.6rem);
  line-height: 1.03;
  letter-spacing: -0.035em;
  max-width: 10ch;
}
.hero p {
  margin: 24px 0 0;
  color: #344866;
  font-size: 1.12rem;
  line-height: 1.72;
  max-width: 58ch;
}
.hero-actions, .form-actions {
  display: flex;
  gap: 14px;
  flex-wrap: wrap;
  margin-top: 28px;
}
.btn {
  min-height: 56px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0 28px;
  border-radius: 18px;
  font-weight: 700;
  border: 1px solid transparent;
  cursor: pointer;
}
.btn-primary {
  background: linear-gradient(180deg, #e2bd67 0%, #c79f52 100%);
  color: #1b2e4e;
  box-shadow: 0 10px 24px rgba(199,159,82,.20);
}
.btn-secondary,
.btn-secondary-plain {
  background: #ffffff;
  border-color: rgba(24,39,66,.12);
  color: var(--navy);
}
.hero-image-card {
  min-width: 0;
  overflow: hidden;
  border-radius: 24px;
  border: 1px solid rgba(199,159,82,.18);
  box-shadow: var(--shadow);
  background: #fff;
}
.hero-image-card img {
  width: 100%;
  display: block;
  object-fit: cover;
}

.section {
  padding: 70px 0;
}
.section-white {
  background: #ffffff;
}
.section-soft {
  background: #fbfbfd;
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
}
.about-grid,
.contact-grid,
.market-grid {
  display: grid;
  gap: 36px;
  align-items: start;
}
.about-grid {
  grid-template-columns: .92fr 1.08fr;
  align-items: stretch;
}
.market-grid,
.contact-grid {
  grid-template-columns: .92fr 1.08fr;
}

.section-label {
  color: #8f713b;
  font-size: .84rem;
  text-transform: uppercase;
  letter-spacing: .26em;
  font-weight: 700;
}
.section h2 {
  margin: 18px 0 22px;
  color: var(--navy);
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(2.1rem, 3.8vw, 3.3rem);
  line-height: 1.08;
  letter-spacing: -0.03em;
  max-width: 12ch;
}
.about-copy p,
.contact-lead {
  color: var(--muted);
  font-size: 1.08rem;
  line-height: 1.72;
  max-width: 36ch;
}
.value-panel {
  margin-top: 28px;
  border: 1px solid rgba(199,159,82,.26);
  background: #ffffff;
  border-radius: 22px;
  padding: 22px 24px;
  box-shadow: var(--shadow);
}
.panel-label {
  color: #8f713b;
  font-size: .84rem;
  text-transform: uppercase;
  letter-spacing: .22em;
  font-weight: 700;
}
.value-list {
  margin: 16px 0 0;
  padding: 0;
  list-style: none;
  display: grid;
  gap: 14px;
}
.value-list li {
  position: relative;
  padding-left: 18px;
  color: #24354f;
  line-height: 1.6;
}
.value-list li::before {
  content: "";
  position: absolute;
  left: 0;
  top: 11px;
  width: 7px;
  height: 7px;
  border-radius: 999px;
  background: var(--gold);
}
.about-image-card {
  border-radius: 22px;
  overflow: hidden;
  box-shadow: var(--shadow);
  border: 1px solid rgba(24,39,66,.08);
  background: #fff;
}
.about-image-card img {
  width: 100%;
  height: 100%;
  min-height: 340px;
  display: block;
  object-fit: cover;
}

.card-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 22px;
  margin-top: 34px;
}
.info-card,
.office-card,
.contact-form {
  border: 1px solid rgba(24,39,66,.08);
  border-radius: 24px;
  background: #ffffff;
  box-shadow: var(--shadow);
}
.info-card { padding: 26px; }
.info-card h3 {
  margin: 0;
  color: var(--navy);
  font-family: Georgia, "Times New Roman", serif;
  font-size: 1.18rem;
}
.info-card ul {
  margin: 16px 0 0;
  padding-left: 18px;
  color: var(--muted);
  line-height: 1.82;
}

.market-tags {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  align-items: center;
}
.market-tags span {
  border: 1px solid rgba(24,39,66,.12);
  border-radius: 999px;
  padding: 10px 16px;
  color: var(--navy);
  background: #ffffff;
  box-shadow: 0 6px 18px rgba(10,20,35,.04);
}

.office-card {
  margin-top: 26px;
  padding: 24px;
  color: var(--muted);
  line-height: 1.8;
  max-width: 500px;
}
.office-card strong {
  display: block;
  margin-bottom: 10px;
  color: var(--navy);
  font-size: 1.05rem;
}
.contact-form {
  padding: 28px;
}
.form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}
.form-grid .full { grid-column: 1 / -1; }
.form-grid input,
.form-grid textarea {
  width: 100%;
  border: 1px solid rgba(21, 37, 61, 0.12);
  border-radius: 18px;
  background: #f7f9fc;
  padding: 15px 16px;
  color: var(--text);
}
.form-grid textarea {
  min-height: 182px;
  resize: vertical;
}
.form-status {
  margin-top: 18px;
  border-radius: 16px;
  padding: 13px 16px;
  font-size: .98rem;
}
.form-status.success {
  background: #eaf8ee;
  color: #166534;
  border: 1px solid rgba(22, 101, 52, 0.12);
}
.form-status.error {
  background: #fef2f2;
  color: #b91c1c;
  border: 1px solid rgba(185, 28, 28, 0.12);
}

.whatsapp-float {
  position: fixed;
  right: 26px;
  bottom: 24px;
  z-index: 40;
  width: 82px;
  height: 82px;
  border-radius: 999px;
  background: #2cbf55;
  color: white;
  display: grid;
  place-items: center;
  box-shadow: 0 16px 32px rgba(44,191,85,.22);
  border: 6px solid rgba(255,255,255,.92);
}
.wa-ring {
  font-size: 2rem;
  line-height: 1;
}

@media (max-width: 1100px) {
  .hero-grid,
  .about-grid,
  .card-grid,
  .market-grid,
  .contact-grid {
    grid-template-columns: 1fr;
  }
  .section h2,
  .hero h1,
  .hero p,
  .about-copy p,
  .contact-lead {
    max-width: none;
  }
}

@media (max-width: 760px) {
  .container { width: min(var(--container), calc(100% - 24px)); }
  .nav { display: none; }
  .brand { font-size: 1.55rem; }
  .brand-sub { font-size: .78rem; letter-spacing: .24em; }
  .hero h1 { font-size: clamp(2.4rem, 10vw, 4rem); }
  .form-grid { grid-template-columns: 1fr; }
  .whatsapp-float {
    right: 14px;
    bottom: 14px;
    width: 72px;
    height: 72px;
  }
}
